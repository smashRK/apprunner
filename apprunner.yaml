version: 1.0
runtime: python3
build:
  echo: 'building app...'
  commands:
    build:
      - pip install -r requirements.txt
run:
  echo: 'running app...'
  command: gunicorn app:app
  network:
    port: 8080